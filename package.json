{
  "name": "jxnfilmclub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "nue serve",
    "build": "nue build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "nuekit": "2.0.0-beta.2",
    "vitest": "^2.1.0",
    "wrangler": "^3.80.0"
  }
}
