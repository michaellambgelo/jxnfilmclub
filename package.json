{
  "name": "jxnfilmclub",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22 <23"
  },
  "type": "module",
  "scripts": {
    "dev": "nue serve",
    "build": "nue build",
    "postbuild": "rm -rf .dist/admin .dist/tests .dist/tui .dist/worker .dist/scripts .dist/playwright-report .dist/test-results .dist/node_modules .dist/playwright.config.js .dist/vitest.config.js .dist/vitest.workspace.js .dist/package-lock.json .dist/bun.lock",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "admin": "node admin/server.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@playwright/test": "^1.59.1",
    "nuekit": "2.0.0-beta.2",
    "vitest": "^2.1.0",
    "wrangler": "^4.114.0"
  }
}
